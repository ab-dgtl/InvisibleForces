/* ===========================================================
   Настройки сайта «Невидимые опоры»
   -----------------------------------------------------------
   Отправка заявок в Telegram через вебхук Make (вариант 1).
   Секреты бота хранятся на стороне Make, в коде сайта их нет.

   ВАЖНО: всё, что находится в этом файле, видно любому посетителю
   сайта (это клиентский код).
   =========================================================== */
window.SITE_CONFIG = {
  telegram: {
    /* true — уведомления включены */
    enabled: true,

    /* Вебхук Make: принимает POST с JSON, далее сценарий Make
       пересылает сообщение в Telegram. */
    webhookUrl: 'https://hook.eu2.make.com/vis7dvru8h6hgk5di3mibimchjj8yj79',

    /* Вариант 2 (напрямую через Bot API) — не используется.
       Токен здесь был бы виден в исходном коде страницы. */
    botToken: '',
    chatId: ''
  }
};
