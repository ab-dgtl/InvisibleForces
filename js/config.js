/* ===========================================================
   Настройки сайта «Невидимые опоры»
   -----------------------------------------------------------
   Файл нужен для отправки заявок в Telegram.
   Впишите свои значения и поставьте enabled: true.
   Подробная инструкция — в README.md (раздел «Отправка в Telegram»).

   ВАЖНО: всё, что находится в этом файле, видно любому посетителю
   сайта (это клиентский код). Поэтому:
   • вариант 1 (webhookUrl) — ПРЕДПОЧТИТЕЛЬНЫЙ: секреты хранятся
     на стороне сервиса-посредника;
   • вариант 2 (botToken + chatId) — работает без сервера, но токен
     бота становится публичным. Используйте только тестового бота.
   =========================================================== */
window.SITE_CONFIG = {
  telegram: {
    enabled: false,

    /* Вариант 1 (рекомендуется): URL вашего вебхука/прокси или
       сервиса-посредника (Make, n8n, Zapier, Cloudflare Worker).
       Сайт отправит заявку туда, а сервис — уже в Telegram. */
    webhookUrl: '',

    /* Вариант 2 (без сервера): данные бота Telegram.
       Токен бот получает у @BotFather, chat_id — у @userinfobot
       или из метода getUpdates. Токен будет виден в исходном коде. */
    botToken: '',
    chatId: ''
  }
};
